% IMTRANSLATE    Loads the given image stack into memory, applying row/column shifts
  (rigid translation) to each frame. If sub-pixel registration is
  requested, cv::warpAffine() is used.
%
%  Usage syntax:
%    image = imtranslate( inputPath, dx, dy, [methodInterp = -1] );
%
%  Author:   Sue Ann Koay (koay@princeton.edu)
%
